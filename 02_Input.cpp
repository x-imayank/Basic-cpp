#include<iostream>
using namespace std; // We define std for all cout.

int main(){
    int x;
    cout <<"Enter the input: ";
    cin >> x;     //cin in used for taking input from the user.
    cout << "Value of x : " << x << endl;
    return 0;
}